# Probe manifests and related Stage 0 cluster fixtures

- `../registry/` — in-cluster `registry:2` + ground-truth layer image build Job (section 2.5 layer-share, reusable for 2.6)
- UID probe workloads: still held on the TTFS client-behavior decision
