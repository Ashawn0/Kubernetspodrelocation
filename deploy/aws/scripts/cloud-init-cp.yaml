#cloud-config
hostname: ${hostname}
manage_etc_hosts: true
package_update: true
packages:
  - jq
  - iperf3
  - iproute2
runcmd:
  - [bash, -c, "echo 'reloc-disrupt cp cloud-init done' > /var/log/reloc-cloud-init.done"]
