# Experiment configs and results

`results/calibration/` is for oracle-target selection later.
`results/evaluation/` is for regret later.
Never mix replica IDs across those two. Stage 0 traces go only in `results/stage0/`.
